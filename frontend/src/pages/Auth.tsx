import { useState } from "react";
import { useLocation, useSearch } from "wouter";
import { ArrowLeft, ArrowRight, CalendarDays, Check, Eye, EyeOff, LockKeyhole, Mail, Sparkles, Ticket, UserRound } from "lucide-react";

export default function Auth() {
  const [, navigate] = useLocation();
  const query = useSearch();
  const params = new URLSearchParams(query);
  const event = params.get("event");
  const initialRole = params.get("role") === "organizer" ? "organizer" : "attendee";
  const [role, setRole] = useState<"attendee" | "organizer">(initialRole);
  const [mode, setMode] = useState<"signup" | "login">("signup");
  const [showPassword, setShowPassword] = useState(false);
  const [email, setEmail] = useState("");
  const nextPath = role === "attendee" ? "/attendee" : "/organizer";
  return <div className="min-h-screen bg-[#f6f4ee] px-5 py-5 text-ink sm:px-8"><button onClick={() => navigate("/")} className="mx-auto flex w-full max-w-[1240px] items-center gap-2 text-[12px] font-bold text-ink/55 hover:text-ink"><ArrowLeft className="size-4" /> Back to events</button><main className="mx-auto grid min-h-[calc(100vh-80px)] max-w-[1060px] items-center gap-8 py-10 lg:grid-cols-[0.92fr_1.08fr]"><section className="hidden rounded-[30px] bg-ink p-10 text-white shadow-[0_24px_52px_rgba(14,40,49,0.18)] lg:block"><div className="grid size-11 place-items-center rounded-[14px] bg-coral text-ink"><Sparkles className="size-5" /></div><p className="mt-12 text-[10px] font-black uppercase tracking-[0.2em] text-coral">Your next room awaits</p><h1 className="mt-4 font-display text-[54px] font-bold leading-[0.92] tracking-[-0.08em]">Make space<br />for <span className="text-coral">ideas.</span></h1><p className="mt-6 max-w-[320px] text-[13px] leading-6 text-white/55">Join EventForge to discover events, book your place, and keep every moment in one beautiful workspace.</p><div className="mt-14 space-y-3 text-[11px] font-bold text-white/65"><p className="flex items-center gap-2"><Check className="size-4 text-mint" /> Your tickets, always within reach</p><p className="flex items-center gap-2"><Check className="size-4 text-mint" /> Sessions matched to your interests</p><p className="flex items-center gap-2"><Check className="size-4 text-mint" /> One calm place for event details</p></div></section><section className="mx-auto w-full max-w-[470px] rounded-[28px] border border-white bg-white/65 p-6 shadow-[0_20px_46px_rgba(47,59,61,0.08)] backdrop-blur-xl sm:p-8"><div className="mb-7"><p className="text-[10px] font-black uppercase tracking-[0.18em] text-coral">Welcome to EventForge</p><h2 className="mt-2 font-display text-[31px] font-bold tracking-[-0.07em]">{mode === "signup" ? "Create your space." : "Welcome back."}</h2><p className="mt-2 text-[12px] text-ink/50">{event ? <>Sign in to continue to <b className="text-ink">{event}</b>.</> : "Choose how you use EventForge to continue."}</p></div><div className="mb-6 grid grid-cols-2 gap-2 rounded-[14px] bg-ink/5 p-1"><button onClick={() => setRole("attendee")} className={`flex items-center justify-center gap-2 rounded-[11px] py-2.5 text-[11px] font-black transition ${role === "attendee" ? "bg-white text-ink shadow-sm" : "text-ink/45"}`}><Ticket className="size-3.5" /> Attendee</button><button onClick={() => setRole("organizer")} className={`flex items-center justify-center gap-2 rounded-[11px] py-2.5 text-[11px] font-black transition ${role === "organizer" ? "bg-white text-ink shadow-sm" : "text-ink/45"}`}><CalendarDays className="size-3.5" /> Organizer</button></div><form onSubmit={(e) => { e.preventDefault(); navigate(nextPath); }} className="space-y-3">{mode === "signup" && <label className="block"><span className="mb-1.5 block text-[10px] font-black uppercase tracking-[0.12em] text-ink/45">Full name</span><span className="relative block"><UserRound className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-ink/30" /><input required placeholder="Olivia Adams" className="h-11 w-full rounded-[12px] border border-ink/8 bg-white/65 pl-10 pr-3 text-[12px] outline-none transition focus:border-coral/60 focus:ring-4 focus:ring-coral/10" /></span></label>}<label className="block"><span className="mb-1.5 block text-[10px] font-black uppercase tracking-[0.12em] text-ink/45">Work email</span><span className="relative block"><Mail className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-ink/30" /><input required type="email" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="you@company.com" className="h-11 w-full rounded-[12px] border border-ink/8 bg-white/65 pl-10 pr-3 text-[12px] outline-none transition focus:border-coral/60 focus:ring-4 focus:ring-coral/10" /></span></label><label className="block"><span className="mb-1.5 block text-[10px] font-black uppercase tracking-[0.12em] text-ink/45">Password</span><span className="relative block"><LockKeyhole className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-ink/30" /><input required type={showPassword ? "text" : "password"} placeholder="At least 8 characters" className="h-11 w-full rounded-[12px] border border-ink/8 bg-white/65 pl-10 pr-10 text-[12px] outline-none transition focus:border-coral/60 focus:ring-4 focus:ring-coral/10" /><button type="button" onClick={() => setShowPassword((v) => !v)} className="absolute right-3 top-1/2 -translate-y-1/2 text-ink/35">{showPassword ? <EyeOff className="size-4" /> : <Eye className="size-4" />}</button></span></label><button className="mt-2 flex h-11 w-full items-center justify-center gap-2 rounded-[12px] bg-coral text-[12px] font-black text-ink shadow-[0_10px_18px_rgba(240,123,103,0.2)] transition hover:-translate-y-0.5">{mode === "signup" ? `Continue as ${role === "attendee" ? "attendee" : "organizer"}` : "Sign in"}<ArrowRight className="size-4" /></button></form><p className="mt-5 text-center text-[11px] text-ink/45">{mode === "signup" ? "Already have an account?" : "New to EventForge?"} <button onClick={() => setMode(mode === "signup" ? "login" : "signup")} className="font-black text-[#a65745]">{mode === "signup" ? "Sign in" : "Create one"}</button></p></section></main></div>;
}
